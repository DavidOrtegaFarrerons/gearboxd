info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{domain}}/v1/users"
  body:
    type: json
    data: |-
      {
        "email": "user@gearboxd1.com",
        "username": "user1",
        "password": "123456781"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
